# V333 Compact guest desktop More menu

- Resets the global section padding that made desktop More groups excessively tall.
- Uses a 300px compact dropdown with 42px minimum rows and left-aligned labels.
- Pins public arrows and member locks to the row edge.
- Applies active styling across the full row and preserves RTL behavior.
