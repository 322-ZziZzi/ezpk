# v353 인원수 기반 방향별 자동 배치

## 공통

- 1~72번은 프랭키 주변의 기존 순환 배치를 유지합니다.
- 73번부터는 실제 배치 인원수와 선택 방향에 맞는 바깥쪽 슬롯을 사용합니다.
- 연맹원 새로고침만으로 위치를 변경하지 않으며 `자동 배치` 실행 시에만 다시 계산합니다.
- 위치 고정된 연맹원은 현재 좌표를 유지하고 나머지 연맹원만 새 슬롯에 배치합니다.
- 수동 교환, 임시 저장, 저장 상태로 되돌리기와 게시 흐름은 기존과 동일합니다.
- 100명 완성본은 기존 확정 템플릿을 유지합니다.

## 우

- 위쪽은 오른쪽 정렬, 왼쪽 측면, 아래쪽 순서로 채웁니다.
- 아래쪽은 오른쪽부터 사용하므로 인원이 부족하면 왼쪽부터 비어 있습니다.

## 좌

- 위쪽은 왼쪽 정렬, 오른쪽 측면, 아래쪽 순서로 채웁니다.
- 아래쪽은 왼쪽부터 사용하므로 인원이 부족하면 오른쪽부터 비어 있습니다.

## 상

- 왼쪽 바깥 열을 위에서 아래로 채웁니다.
- 이어서 아래쪽 첫 번째 줄, 맨 아래 줄을 왼쪽에서 오른쪽으로 채웁니다.

## 하

- 왼쪽 바깥 열을 아래에서 위로 채웁니다.
- 이어서 위쪽의 아래 줄, 맨 위 줄을 왼쪽에서 오른쪽으로 채웁니다.
