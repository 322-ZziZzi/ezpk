# EZPK v311 수도전 노출 명단 복원 기능

## 변경 내용
- 수도전 관리 도구 모음에 `현재 노출 명단 불러오기` 버튼 추가
- D1의 `published.teams`에 저장되어 현재 수도전 페이지에 노출 중인 명단을 `draft` 편집 목록으로 복원
- 불러올 때 참가자 목록, 팀별 배정, 고정 팀 정보를 함께 재구성
- 현재 편집 중인 목록이 있으면 덮어쓰기 확인창 표시
- 불러오기만으로 D1을 즉시 변경하지 않으며, 관리자가 `저장`을 눌러야 최종 반영

## 데이터베이스
- 기존 `strategy_content` JSON 구조를 그대로 사용하므로 신규 D1 마이그레이션 없음
